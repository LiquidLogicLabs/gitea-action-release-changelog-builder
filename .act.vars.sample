# Sample Act variables for E2E tests
# Copy this file to .act.vars and fill in your values
# These map to GitHub Actions variables (vars.*)
# Format: KEY=value (simple key=value pairs, no bash syntax)

# Test repository (owner/repo format or URL)
# Used for the "explicit-vars-repo" matrix scenario
TEST_GITHUB_REPO=owner/repo

# Test tags to use for changelog generation
# Note: fromTag should be older, toTag should be newer
# Example: v3.6.5 (older) → v3.6.6 (newer/latest)
TEST_GITHUB_FROM_TAG=v3.6.5
TEST_GITHUB_TO_TAG=v3.6.6
