# Secrets for act (do NOT commit real tokens).
# Copy to .act.secrets and set values locally. In CI, these are set in the repo's
# TEST environment (Environment secrets).

# E2E tests - token for GitHub API (repo/tag/release lookups). Fallback: GITHUB_TOKEN.
# TEST_GITHUB_TOKEN=

# Used by workflows that configure private registry auth during npm install.
# NPM_REGISTRY_TOKEN=
